---
const { href } = Astro.props;
const { pathname: path } = new URL(Astro.request.url);
---

<a
  href={href}
  class:list={[
    {
      "font-semibold": path === href,
      "hover:text-blue-500 hover:bg-gray-200 rounded": path !== href,
    },
    "py-2 px-3 mr-1",
  ]}><slot /></a
>
